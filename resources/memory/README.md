# Memory (optional)

Source: `thedotmack/claude-mem` - 3-layer search, worker daemon.

- File checkpoint (`PLAN`+`PROGRESS`+`HANDOFF`) is enough for most projects.
- Use `resources/memory/` only for very long multi-session projects.
