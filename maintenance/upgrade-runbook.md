# Upgrade runbook

1. Propose tradeoffs (cost/perf/scale) via `web-fetch` latest docs.
2. Create PLAN with DAG, run AUTONOMOUS or NORMAL (L22).
3. Prefer referencing latest version over dumping old info (L24).
4. If new LLM proposes improvement, file std format (L25).

Verify: `pnpm outdated`, `lint`, `test`, `build`.
